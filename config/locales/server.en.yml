en:
  site_settings:
    algolia_enabled: "Enable the discourse-algolia plugin"
    algolia_application_id: "The Algolia app ID where you want the data sent"
    algolia_admin_api_key: "An Algolia API key with write and index setting permissions"
    algolia_search_api_key: "An Algolia API key with only search permission, used for the front end"
    algolia_discourse_username: "Name of user act as when fetching content"
    algolia_searchbar: "Enable the Algolia search bar"
